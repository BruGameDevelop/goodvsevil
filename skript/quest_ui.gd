# QuestUI.gd (z. B. als Teil deines HUDs)
extends Control
#@onready var quest_popup = $QuestPopup

func show_quest_popup(quest: QuestResource):
	#quest_popup.get_node("Title").text = quest.title
	#quest_popup.get_node("Description").text = quest.description
	#quest_popup.visible = true
	#$QuestPopupTimer.start()
	pass
